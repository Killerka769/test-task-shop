import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['example.com', 'o-complex.com', 'placehold.co', 'picsum.photos', 'dummyimage.com'],
  },
};

export default nextConfig;
